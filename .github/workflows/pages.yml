name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false

      - name: Enable pinned pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.19.0 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run pre-deploy quality gates
        run: |
          pnpm test
          pnpm run typecheck
          pnpm run lint
          pnpm run build

      - name: Build Pages artifact
        run: pnpm run build:pages

      - name: Validate Pages artifact
        run: pnpm run validate:pages

      - name: Install browser engines for deployment E2E
        run: pnpm exec playwright install --with-deps chromium webkit

      - name: Run repository and iPhone deployment gates
        run: |
          pnpm run test:e2e
          pnpm run test:pages
          pnpm run test:iphone

      - name: Run managed WebKit iPhone deployment gate
        env:
          WW_BROWSER_TYPE: webkit
        run: pnpm run test:iphone

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 10
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
